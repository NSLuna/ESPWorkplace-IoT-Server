#pragma once
// 이 파일을 secrets.h 로 복사한 뒤 실제 값을 채운다.
// secrets.h 는 .gitignore 에 등록되어 저장소에 올라가지 않는다.

// ── WiFi ───────────────────────────────────────────────────
#define WIFI_SSID       "여기에_SSID"
#define WIFI_PASSWORD   "여기에_비밀번호"

// ── MQTT 브로커 ────────────────────────────────────────────
#define MQTT_BROKER     "broker.example.com"
#define MQTT_PORT       1883
#define MQTT_USER       ""        // 인증 없으면 빈 문자열
#define MQTT_PASS       ""
